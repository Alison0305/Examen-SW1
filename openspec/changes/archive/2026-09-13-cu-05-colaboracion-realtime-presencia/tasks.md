## 1. Incremento 1 — Transporte autorizado, comandos y persistencia autoritativa

- [x] 1.1 Revisar los contratos existentes de JWT, acceso OWNER/EDITOR/VIEWER, `ProjectDocument`, revisión externa, `UmlCommand`, Command Bus y validación; definir envelopes Socket.IO tipados y verificar compilación de contratos sin exponer identidad controlable por cliente.
- [x] 1.2 Añadir el gateway Socket.IO con autenticación JWT durante handshake, unión/salida de sala por proyecto y rechazo uniforme de token o acceso inválido; verificar con pruebas de conexión autenticada y no autorizada.
- [x] 1.3 Reutilizar la autorización de proyectos para limitar observación a miembros vigentes y operaciones mutables a OWNER/EDITOR; verificar matriz OWNER/EDITOR/VIEWER/NONE sin filtrar documento ni presencia.
- [x] 1.4 Ejecutar cada intención recibida mediante `UmlCommand`, Command Bus y validador contra el documento autoritativo; verificar comandos válidos, diagnósticos de rechazo y que React Flow no participa como dominio.
- [x] 1.5 Implementar aplicación serializada por proyecto con `baseRevision`, `Project.revision` como revisión externa autoritativa, actualización condicionada y persistencia antes de confirmación/broadcast; verificar concurrencia de dos clientes, incremento único de revisión y recuperación HTTP consistente.
- [x] 1.6 Añadir mediante migración aditiva el recibo persistente `ProjectOperationReceipt` con sus campos definidos, unicidad `(projectId, operationId)` e índice de expiración; exigir `operationId` nuevo por intención nueva. Implementar una transacción que, tras autorizar, consulte primero el recibo: replay válido o colisión si no expiró; eliminación segura si expiró y después flujo normal de revisión; actualización condicionada proyecto+recibo atómica. Usar SHA-256 de actor/proyecto/revisión base/comando canónico, ack mínimo, vencimiento a 24 horas y limpieza oportunista. Verificar autorización antes del recibo, replay de misma huella, colisión inválida, recibo vencido con `baseRevision` antigua y conflicto/resync, reinicio controlado y que la unicidad como última defensa permite como máximo una ejecución concurrente.
- [x] 1.7 Definir eventos de aceptación, rechazo, conflicto y resync, y broadcast aislado por sala sin documento completo por edición; verificar orden de revisiones, aislamiento entre proyectos y payloads mínimos.
- [x] 1.8 Ejecutar lint, typecheck y pruebas del incremento, y documentar contratos reales, decisiones y límites sin declarar CU-05 completado.

## 2. Incremento 2 — Presencia, workspace y recuperación resiliente

- [x] 2.1 Implementar presencia efímera por sala con identidad pública mínima, avatar, online/offline, selección, cursor, elemento editado y última actividad; propagar unión/salida, selección y edición inmediatamente. Verificar conexión, actualización, desconexión y ausencia de persistencia de dominio, cambios en `ProjectDocument` o revisión.
- [x] 2.2 Limitar cursor por participante a máximo 10 emisiones/segundo y mínimo 100 ms, coalesciendo el estado más reciente; limitar actividad a máximo una emisión cada 5 segundos. Verificar 50 eventos de cursor en un segundo, límites, coalescencia, actividad y que presencia no persiste, no entra al Command Bus, no crea recibos ni bloquea operaciones UML.
- [x] 2.3 Integrar el cliente `socket.io-client` en el workspace persistido con estado de conexión, revisión confirmada, unión/salida segura y proyección de operaciones remotas; verificar que cada actualización autoritativa refresca el documento sin una segunda ruta de mutación.
- [x] 2.4 Mostrar avatares, estados online/offline, selección resaltada, cursor remoto y elemento editado conforme a la identidad visual existente; verificar renderizado de múltiples colaboradores y limpieza al salir.
- [x] 2.5 Sustituir el guardado manual divergente por estado de guardado inmediato tras confirmación realtime; verificar que la UX muestra revisión sincronizada y no ejecuta PUT adicional por una operación aceptada.
- [x] 2.6 Implementar UX de conflicto, salto de revisión y resync explícito que bloquea nuevas mutaciones hasta hidratar la respuesta autoritativa; verificar que no hay sobrescritura, merge ni reintento ciego.
- [x] 2.7 Implementar reconexión segura que consulta el resultado por `operationId` o ejecuta resync antes de reanudar edición; verificar pérdida/restauración de conexión y operación ambigua.
- [x] 2.8 Auditar y sustituir el Undo/Redo colaborativo basado en snapshots completos por `RestoreDeletionSnapshot`: definir la unión `DeletionSnapshot` para clase+relaciones incidentes+layout, enumeración+layout, relación+layout, atributo y literal; conservar cada elemento y layout en su índice original de toda colección observable (`classes`, `enumerations`, `relationships`, `attributes`, `literals`, `layout.elements`) sin inventar índices no observables. Capturarla solo al aceptar `DeleteClass`, `DeleteEnumeration`, `DeleteRelationship`, `RemoveAttribute` o `RemoveEnumerationLiteral`; validar antes de mutar que los índices sean enteros seguros y estén dentro del rango de inserción, rechazar de forma determinista los índices inválidos y restaurar el candidato atómicamente mediante Command Bus. Distinguir conflicto de `baseRevision` frente a rechazo semántico. En colaboración, enviar Undo y Redo como operaciones nuevas con `operationId` nuevo, actualizar historial solo tras ACK y limpiarlo ante remoto, conflicto, rechazo o resync; verificar que ningún snapshot local modifica una revisión remota.
- [x] 2.9 Ejecutar pruebas frontend del incremento y una prueba manual con dos navegadores autorizados para edición, presencia, conflicto, resync, reconexión y permisos; documentar evidencia y limitaciones reales.

## 3. Incremento 3 — Verificación multi-cliente y cierre de CU

- [x] 3.1 Ampliar pruebas de `uml-core`, backend Socket.IO e integración para handshake JWT, autorización previa a recibos, autorización de sala, roles y ejecución/validación de comandos. Cubrir `AddAttribute`/`RemoveAttribute` y `AddEnumerationLiteral`/`RemoveEnumerationLiteral`, captura solo tras eliminación aceptada y `RestoreDeletionSnapshot` atómico de clase+relaciones incidentes+layout, enumeración+layout, relación+layout, atributo y literal, conservando el orden de clases, enumeraciones, relaciones, atributos, literales y layouts cuando sea observable. Verificar rechazo determinista de índices negativos, no enteros, no seguros y fuera de rango, además de snapshots incompletos, duplicados o incompatibles, sin mutación parcial. Cubrir conflicto stale previo al executor, migración/atomicidad de proyecto+recibo, consulta transaccional inicial de recibo, replay por huella, colisión, eliminación segura de expirado, unicidad con máximo una ejecución concurrente, broadcast, conflicto stale y resync; verificar que rechazo no incrementa revisión ni crea recibo y que conflicto no ejecuta el comando.
- [x] 3.2 Ampliar pruebas frontend para estado de conexión, presencia remota, cursor con 50 eventos y coalescencia/límites, selección/edición/unión/salida inmediatas, actividad limitada a 5 segundos, ausencia de efectos de dominio, revisión/guardado confirmado, conflicto, resync y reconexión. Para Undo/Redo colaborativo, verificar exactamente que Undo de clase, enumeración, relación, atributo y literal envía su `RestoreDeletionSnapshot` indexado como operación nueva con `operationId` nuevo; que Redo reemite la operación semántica nueva con otro `operationId`; que documento e historial cambian solo tras ACK; y que remoto, conflicto, rechazo de restauración o resync invalidan el historial sin sustituir un snapshot local sobre estado remoto.
- [x] 3.3 Añadir prueba E2E o multi-cliente equivalente para dos miembros autorizados que editan el mismo proyecto, reciben operaciones y presencia, resuelven un stale por resync y conservan el documento tras recargar.
- [x] 3.4 Ejecutar gates de raíz, Prisma validate/migrate status para la migración de recibos, pruebas relevantes y validación OpenSpec strict; registrar conteos y resultados reales.
- [x] 3.5 Actualizar `docs/puds/use-cases/CU-05-*.md`, `docs/STATUS.md` y `docs/HANDOFF.md` con implementación, pruebas, errores, decisiones, límites y siguiente acción, sin archivar antes de aceptación explícita.
- [x] 3.6 Realizar prueba manual final con OWNER, EDITOR y VIEWER, confirmar presencia y recuperación, solicitar aceptación explícita y solo después preparar verify, archive, commit y push conforme al flujo del CU.
