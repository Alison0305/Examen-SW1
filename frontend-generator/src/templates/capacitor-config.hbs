import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.examen.sw1.generated",
  appName: "Examen SW1",
  webDir: "out",
  server: { androidScheme: "http" },
};

export default config;
