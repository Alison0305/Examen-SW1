## 1. Relational layer
- [x] 1.1 Create the approved relational workspace and immutable RelationalModel types with source references.
- [x] 1.2 Implement deterministic UML-to-relational mapping, naming validation and blocking generation diagnostics.
- [x] 1.3 Implement approved primitive, enum, PK, constraint, multiplicity, association, aggregation, composition and inheritance rules.
- [x] 1.4 Add mapper tests for supported mappings, gaps, diagnostics and deterministic ordering.

## 2. Spring generator
- [x] 2.1 Create the approved generator workspace and Handlebars dependency/templates.
- [x] 2.2 Generate deterministic GeneratedFile arrays for Gradle, application, entities, enums, repositories, services and structural controllers.
- [x] 2.3 Implement safe output writing and generator tests for paths, imports, annotations, duplicates and traversal.

## 3. Representative compilation
- [x] 3.1 Add the representative UML fixture and semantic generator assertions.
- [x] 3.2 Generate to controlled temporary output and compile with Gradle Wrapper and Java 21.
- [x] 3.3 Run gates, perform manual generation review, document decisions and validate OpenSpec.
